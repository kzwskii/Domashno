using Microsoft.EntityFrameworkCore;
using static System.Windows.Forms.VisualStyles.VisualStyleElement;
using static System.Windows.Forms.VisualStyles.VisualStyleElement.Button;

namespace RegisterSystem
{
    public partial class RegisterForm : Form
    {
        public RegisterForm()
        {
            InitializeComponent();
        }

        private void btnRegister_Click(object sender, EventArgs e)
        {
            if (RegisterEventHelpers.CheckAllFields(
                    txtUsername.Text, 
                    txtPassword.Text, 
                    txtConfirmPassword.Text))
            {
                MessageBox.Show(
                    "All fields must have a value!",
                    "Error",
                    MessageBoxButtons.OK,
                    MessageBoxIcon.Error
                );

                return;
            }

            if (txtPassword.Text != txtConfirmPassword.Text)
            {
                MessageBox.Show(
                    "Passwords must match!",
                    "Error",
                    MessageBoxButtons.OK,
                    MessageBoxIcon.Error
                );

                return;
            }

            if (txtPassword.TextLength < 8 ||
                txtConfirmPassword.TextLength < 8)
            {
                MessageBox.Show(
                    "Password must have at least 8 chars",
                    "Error",
                    MessageBoxButtons.OK,
                    MessageBoxIcon.Error
                );

                return;
            }

            /*
                1. Открийте проблемите с така написаните репозитории
                2. Допишете след това логиката за регистриране 
                3. И за логване направете логиката за логинг
                4. Когато успешно се регистрира потребител да се показва 
                   логин формата и да се скрива регистрационната форма(Hide)
                5. Ще възникне проблем със затварянето на приложението
                   поинтересувайте се какво е FormClosed += (sender, e) => this.Close()
                    ИЗПОЛЗВАЙТЕ МЕСИДЖ КУТИИ НАВСЯКЪДЕ ЗА ДА УВЕДОМИТЕ ПОТРЕБИТЕЛЯ 
            КАКВО СЕ СЛУЧВА!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!
                6. Качете в гитхъб. 
            */
        }

        private void chbShowPassword_CheckedChanged(object sender, EventArgs e)
        {
            if (chbShowPassword.Checked)
            {
                txtPassword.UseSystemPasswordChar = false;
                txtConfirmPassword.UseSystemPasswordChar = false;
            }
            else
            {
                txtPassword.UseSystemPasswordChar = true;
                txtConfirmPassword.UseSystemPasswordChar = true;
            }
        }
    }
}
